# NTC Fleet Management Environment Configuration (Example)
# DO NOT place actual secrets in this file. Copy this to `.env` and fill in the values.

# Backend Configuration
PORT=5000
NODE_ENV=development

# JWT Secrets for Auth
JWT_SECRET=ntc_super_secret_fleet_key_2026

# Database Path
# DB_PATH=./ntc_vms.db

# PostgreSQL Connection String (Replace <password> with actual password)
DATABASE_URL="postgresql://neondb_owner:<password>@ep-twilight-resonance-ai2x6l6a.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require"
